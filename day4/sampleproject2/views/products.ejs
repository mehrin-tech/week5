<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900">
    <div class="mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold text-center mb-6">Our Products</h1>
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
          <%products.forEach(product=>{%>
              <div class="bg-gray-100 shadow-md rounded-3xl p-4">
                <img src="<%= product.image %>" alt="<%= product.name %>" class="w-full h-30 object-cover rounded-xl mb-4">
                <h2 class="text-xl font-semibold"><%= product.name %></h2>
                <p class="text-green-600 font-bold mt-2">₹<%=product.price %></p>
            </div>
         <% })%>
           
        </div>
    </div>
</body>
</html>